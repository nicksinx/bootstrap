schema_version: 1
profile_version: "2.0.0"
name: forge-lifecycle
extends: default
description: >
  DEPRECATED alias for profile `default` v2.0.0. Forge MCP and OKF dispatch are
  included in the default profile since bootstrap alignment v2.
files:
  include: []
  exclude: []
compat_mode: strict
