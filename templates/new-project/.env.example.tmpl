# Environment variables for __PROJECT_NAME__.
# Copy to .env and fill in. .env is gitignored. Never commit secrets.

# MCP server auth (used when --mcp-auth-profile=local-token).
MCP_AUTH_TOKEN=

# Optional: GitHub token for `gh` if not already configured globally.
# GH_TOKEN=
